# Adding a dependency (upper case name)

The guard has to be case-insensitive, or this file's examples become targets.

```python
python_requirement(name="upper", requirements=["tornado==6.3.0"])
```
